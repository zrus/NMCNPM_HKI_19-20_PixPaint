// var objToExport = {
//     PI = 3.14,
//     sum: (a, b) => a + b,
//     sub: (a, b) => a - b,
//     mul: (a, b) => a * b,
// }

// module.exports = objToExport;

module.exports = {
    PI: 3.14,
    sum: (a, b) => a + b,
    sub: (a, b) => a - b,
    mul: (a, b) => a * b,
}